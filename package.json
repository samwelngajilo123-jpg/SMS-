{
  "name": "at-ussd-netlify",
  "version": "1.0.0",
  "description": "Africa's Talking USSD message intake + SMS reply, on Netlify Functions",
  "private": true,
  "scripts": {
    "dev": "netlify dev"
  },
  "dependencies": {
    "africastalking": "^0.7.0",
    "@netlify/blobs": "^7.0.1"
  },
  "devDependencies": {
    "netlify-cli": "^17.0.0"
  }
}
